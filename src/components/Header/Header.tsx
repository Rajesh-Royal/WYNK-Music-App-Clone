import React from "react";

interface Props {

}

const Header = (props: Props) => {
    return (
        <header>
            This is a simple Header
        </header>
    );
};

export default Header;
